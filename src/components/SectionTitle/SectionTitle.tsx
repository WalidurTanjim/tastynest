const SectionTitle = ({ title }: {title: string}) => {
    return <h1 className="text-2xl md:text-3xl lg:text-4xl font-medium text-slate-700 text-center">{title}</h1>;
};

export default SectionTitle;